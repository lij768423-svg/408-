"use strict";
// Main application code was split into ordered classic scripts under assets/js/.
// index.html loads those files directly; this stub remains only to avoid stale references during development.

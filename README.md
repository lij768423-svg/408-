# 408 考研刷题库

轻量级网页刷题应用,基于王道 2026/2027 四本 408 教材的精选题库。

## 快速开始

```bash
# 方式 1:直接双击 index.html 在浏览器中打开
open ~/408-quiz/index.html

# 方式 2:起本地服务器(端口 8767)
cd ~/408-quiz && python3 -m http.server 8767
# 然后访问 http://127.0.0.1:8767/
```

> 💾 所有记录(错题/收藏/答题统计/已答记录)都存在浏览器的 `localStorage` 里,
> 浏览器关闭、重启电脑都不会丢。换浏览器或清空浏览数据会清空。
>
> ⚠️ `file://` 和 `http://127.0.0.1:8767` 在浏览器看来是两个不同的来源(origin),
> `localStorage` 互相隔离 — **请固定使用同一种方式打开**,否则记录会分裂。

## 功能

- **4 本书 + 全部模式**:操作系统 / 数据结构 / 计算机组成原理 / 计算机网络 / 📚 全部 1777 题(跨书随机)
- **章节切换**:每本书可单独选章节,或"全部章节"
- **5 种模式**:
  - 顺序 — 按教材顺序刷
  - 随机 — 打乱顺序
  - 错题 — 只刷做错的
  - 收藏 — 只刷收藏的
  - 全部随机 — 跨书随机出 N 题(可调 20/50/100/200)
- **即时判分开关 ⚡**:
  - 单选题:点 A/B/C/D 立即判分(对/错 + 解析)
  - 多选题:选对继续,选错立即判错,选全立即判对
  - 关闭时:经典「选 → 提交 → 判」流程
- **右侧常驻 AI 讲题栏**:
  - 默认展开,切题不缩放,context 随新题刷新
  - 支持 OpenAI 兼容 / Anthropic / Ollama / 本地 API
  - 流式输出 Markdown + KaTeX 公式(认 `\(...\)` / `\[...\]` / `$...$` / `$$...$$` 四种语法)
  - 追问、复制上下文、配置 API、折叠把手、≤1024px 自动变底部抽屉
  - 底部 mini 学习概览(本组进度 / 正确率 / 本章节错题)
- **4 个选项 + 提交判对错**:立即显示对错 + 正确答案 + 详细解析
- **上一题 / 下一题 / 跳过 / 显示答案 / 收藏(☆ → ★)**
- **错题自动入错题本**,答对的题自动从错题本移除
- **做完一组自动提示「下一组」** — 全部模式下完成 N 题自动出现按钮
- **localStorage 持久化**:错题、收藏、答题统计、最后选的书、即时判分开关、每刷题数、AI 折叠态都自动保存
- **学习记录导出/导入**:JSON 文件,跨设备迁移
- **KaTeX 公式渲染**:题干、解析、AI 输出中的数学公式

## 键盘快捷键

| 键 | 作用 |
|---|---|
| `A` / `B` / `C` / `D` | 选择对应选项(多选题支持累积选择) |
| `Enter` | 提交 / 下一题(根据当前状态) |
| `←` / `→` | 上一题 / 下一题 |
| `Space` | 显示答案 |
| `F` | 收藏 / 取消收藏 |

## 题库规模

- 4 本书 / 26 章 / **1777 题**(单选题 1740 + 多选题 37)
- 数据文件:`data.json` (~2.1 MB)
- 解析覆盖率:100%(每题都有题面 + 4 选项 + 答案 + 解析)

## 重新生成数据

题库数据由 vault 内的提取脚本生成:

```bash
cd ~/Documents/文稿 - dajiji的MacBook Air/Obsidian Vault
python3 wiki/tools/_extract_questions.py
```

脚本会读 `wiki/sources/{操作系统,数据结构,计算机组成原理,计算机网络}/chapters/ch*.md`,
按章节配对「本节习题精选/答案与解析」,提取 4 选项题(单选/多选),
输出到 `~/408-quiz/data.json`。

## 文件结构

```
~/408-quiz/
├── index.html      # 单页应用(内嵌 CSS + JS,~16KB)
├── data.json       # 题库(由 extract 脚本生成)
└── README.md       # 本文件

vault/
└── wiki/tools/
    └── _extract_questions.py   # 提取脚本
```

## 设计原则

- **不污染 vault**:应用和数据都在 vault 外,刷新题库只重跑脚本
- **零依赖**:不需要 Node / npm / 构建步骤
- **离线可用**:数据全在本地 JSON,核心功能无网络请求(AI 讲题可选配外部 API)
- **快速**:3000 题级单文件加载 < 1s,DOM 简单无性能问题
- **单文件结构**:不引入构建工具,改动 index.html 一处即可
- **纸面气质**:米色背景 + 珊瑚色点缀,克制渐变/阴影,长期刷题不累眼

## UI 布局

- **顶栏**:品牌 / 中间金句(每天随机切)/ 右侧 5 项统计(题库 / 已答 / 正确率 / 错题 / 收藏)
- **左侧 sidebar**:书目 → 章节 → 模式 三段式导航
- **中部**:问题卡(题干 / 4 选项 / 操作行 / 答案反馈)
- **右侧常驻 AI 栏**:配置 / context / 输出 / 追问输入 / 学习概览
- **底部**:学习记录自动保存 + 导出/导入

响应式:

- `≥1025px`:3 列布局(左导航 + 中问题卡 + 右 AI 栏)
- `≤1024px`:AI 栏自动变底部抽屉(默认半收,点把手拉起)
- `≤760px`:左导航也折叠为水平按钮

## 已知限制(MVP 范围外)

- ❌ 综合应用题(解答题)未提取(没有 A/B/C/D 选项)
- ❌ 真题年份标签未做专门分类
- ❌ 答题计时
- ❌ 全文搜索
- ❌ 答题统计图

这些功能可在后续迭代添加。

## 浏览器兼容性

需要支持 ES6+ 和 `fetch` API 的现代浏览器(Chrome 121+ / Safari 14+ / Firefox 88+ / Edge 90+)。

> `scrollbar-width: none` 在 Chrome 121+ / Firefox 64+ / Edge 121+ 原生支持;Safari 14+ 用
> `::-webkit-scrollbar { display: none }` 兜底。
